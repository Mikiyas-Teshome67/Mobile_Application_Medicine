name: raey_medicine_delivery
description: "ራዕይ (Ra'ey) - Ethiopia-first medicine delivery super app"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  go_router: ^14.2.7
  firebase_core: ^3.4.1
  firebase_auth: ^5.2.1
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^7.0.1
  google_maps_flutter: ^2.9.0
  geolocator: ^13.0.1
  dio: ^5.7.0
  intl: ^0.19.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
